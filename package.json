{
  "name": "ktor-draw-steel-grimoire",
  "version": "1.0.0",
  "private": true,
  "description": "Draw Steel virtual tabletop companion — TypeScript rewrite (Hono + Drizzle + Angular)",
  "type": "module",
  "packageManager": "pnpm@11.5.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter ./packages/shared build && pnpm --filter ./packages/server build && pnpm --filter ./packages/web build",
    "dev": "pnpm --parallel --filter ./packages/server --filter ./packages/web dev",
    "dev:server": "pnpm --filter ./packages/server dev",
    "dev:web": "pnpm --filter ./packages/web dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "seed": "node seed-campaigns.mjs",
    "db:migrate": "pnpm --filter ./packages/server db:migrate"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
